spring:
  application:
    name: kafkaProj

  datasource:
    url: jdbc:postgresql://training1.effigo.in:5432/training_2
    driver-class-name: org.postgresql.Driver
    username: upeg
    password: sillicon

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  kafka:
    bootstrap-servers: kafka:9092
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.apache.kafka.common.serialization.StringSerializer
    consumer:
      group-id: producer-group-1  # Main consumer group for 'my-topic'
      auto-offset-reset: earliest
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.apache.kafka.common.serialization.StringDeserializer
    topic:
      name: "my-topic"  # Main topic for messages
      fallback-topic: "my-fallback-topic"  # Fallback topic
    consumer-fallback:
      group-id: fallback-consumer-group  # Separate consumer group for 'my-fallback-topic'
      auto-offset-reset: earliest
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.apache.kafka.common.serialization.StringDeserializer

server:
  port: 8081
